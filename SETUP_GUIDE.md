# 📘 دليل الإعداد السريع - نظام حجز ملاعب سفاري

دليل خطوة بخطوة لإعداد وتشغيل النظام من الصفر.

---

## ⏱ الوقت المتوقع: 30-45 دقيقة

---

## 📋 المتطلبات الأولية

قبل البدء، تأكد من توفر:

- [ ] حساب [Supabase](https://supabase.com) (مجاني)
- [ ] حساب [Meta Business](https://business.facebook.com) (مجاني)
- [ ] رقم هاتف واتساب للأعمال
- [ ] محرر نصوص (VS Code موصى به)
- [ ] متصفح حديث (Chrome/Firefox)

---

## 🎯 الخطوة 1: تحميل المشروع

### 1.1 تحميل الملفات

```bash
# إذا كان لديك Git
git clone https://github.com/yourusername/safari-stadium.git
cd safari-stadium

# أو حمّل ZIP وفك الضغط
```

### 1.2 فتح المشروع

```bash
# افتح المجلد في VS Code
code .
```

---

## 🗄 الخطوة 2: إعداد Supabase

### 2.1 إنشاء مشروع جديد

1. اذهب إلى https://app.supabase.com
2. اضغط **"New Project"**
3. املأ البيانات:
   - **Name**: `Safari Stadium`
   - **Database Password**: اختر كلمة مرور قوية (احفظها!)
   - **Region**: اختر أقرب منطقة
4. اضغط **"Create new project"**
5. انتظر 2-3 دقائق حتى يكتمل الإعداد

### 2.2 إنشاء جدول الحجوزات

1. في Dashboard، اذهب إلى **SQL Editor** (من القائمة الجانبية)
2. اضغط **"New query"**
3. افتح ملف `supabase/migrations/001_create_bookings_table.sql`
4. انسخ **كل** محتوى الملف
5. الصق في SQL Editor
6. اضغط **"Run"** (أو Ctrl+Enter)
7. يجب أن ترى رسالة نجاح ✅

### 2.3 التحقق من الجدول

1. اذهب إلى **Table Editor**
2. يجب أن ترى جدول `bookings`
3. اضغط عليه لرؤية الأعمدة

### 2.4 الحصول على API Keys

1. اذهب إلى **Settings** > **API** (من القائمة الجانبية)
2. ستجد:
   - **Project URL**: مثل `https://xxxxx.supabase.co`
   - **anon public**: مفتاح طويل يبدأ بـ `eyJ...`
   - **service_role**: مفتاح آخر (لا تشاركه أبداً!)

3. **احفظ هذه المفاتيح** - ستحتاجها في الخطوة التالية

---

## ⚙️ الخطوة 3: تكوين المشروع

### 3.1 إنشاء ملف البيئة

```bash
# في مجلد المشروع
cp .env.example .env
```

### 3.2 تعديل ملف .env

افتح `.env` وأضف بياناتك:

```env
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGc...الصق المفتاح هنا
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...الصق المفتاح هنا
```

### 3.3 تعديل ملف config.js

افتح `js/config.js` وعدّل:

```javascript
const SUPABASE_CONFIG = {
    url: 'https://xxxxx.supabase.co',  // الصق Project URL
    anonKey: 'eyJhbGc...'                // الصق anon public key
};
```

**⚠️ مهم جداً**: تأكد من إزالة `YOUR_` من القيم!

---

## 🧪 الخطوة 4: اختبار الاتصال

### 4.1 تشغيل الخادم المحلي

اختر طريقة:

```bash
# Python (إذا كان مثبتاً)
python -m http.server 8000

# أو Node.js
npx http-server -p 8000

# أو PHP
php -S localhost:8000

# أو استخدم Live Server في VS Code
```

### 4.2 فتح المتصفح

1. افتح: http://localhost:8000
2. يجب أن ترى الصفحة الرئيسية ✅
3. افتح Console (F12)
4. لا يجب أن ترى أخطاء حمراء

### 4.3 اختبار الحجز

1. اضغط **"احجز الآن"** على أي ملعب
2. املأ النموذج:
   - **الاسم**: محمد أحمد
   - **الجوال**: +966501234567
   - **التاريخ**: اختر غداً
   - **الوقت**: من 16:00 إلى 18:00
3. اضغط **"تأكيد الحجز"**
4. يجب أن ترى رسالة نجاح ✅

### 4.4 التحقق من قاعدة البيانات

1. ارجع إلى Supabase Dashboard
2. **Table Editor** > **bookings**
3. يجب أن ترى الحجز الجديد! 🎉

---

## 👨‍💼 الخطوة 5: اختبار لوحة التحكم

### 5.1 فتح لوحة التحكم

1. افتح: http://localhost:8000/admin.html
2. يجب أن ترى الحجز الذي أنشأته
3. الحالة: **قيد الانتظار**

### 5.2 الموافقة على الحجز

1. اضغط زر **"✓ موافقة"**
2. أكد الموافقة
3. يجب أن تتغير الحالة إلى **مؤكدة** ✅

**ملاحظة**: رسائل واتساب لن تُرسل بعد - سنعدها في الخطوة التالية.

---

## 📱 الخطوة 6: إعداد WhatsApp API (اختياري)

### 6.1 إنشاء حساب Meta Business

1. اذهب إلى https://business.facebook.com
2. اضغط **"Create Account"**
3. املأ بيانات عملك
4. أكمل التحقق

### 6.2 إعداد WhatsApp Business API

1. في Meta Business Suite، اذهب إلى **Settings**
2. اضغط **"Add Assets"** > **"Apps"**
3. اضغط **"Create App"**
4. اختر **"Business"**
5. املأ البيانات وأنشئ التطبيق

### 6.3 إضافة WhatsApp

1. في لوحة التطبيق، ابحث عن **"WhatsApp"**
2. اضغط **"Set up"**
3. اتبع التعليمات لإضافة رقم هاتف
4. **مهم**: استخدم رقم لم يُستخدم في واتساب من قبل

### 6.4 الحصول على Credentials

1. في WhatsApp Setup، اذهب إلى **"API Setup"**
2. ستجد:
   - **Phone Number ID**: رقم طويل
   - **Access Token**: مؤقت (24 ساعة)

### 6.5 إنشاء Access Token دائم

1. اذهب إلى **Settings** > **System Users**
2. أنشئ System User جديد
3. أضف صلاحيات WhatsApp
4. أنشئ Token دائم
5. احفظه في مكان آمن!

### 6.6 إضافة أرقام تجريبية

في بيئة التطوير:

1. **WhatsApp** > **API Setup**
2. **To** > **Manage phone number list**
3. أضف رقمك الشخصي
4. أرسل كود التحقق
5. أدخل الكود

---

## 🚀 الخطوة 7: نشر Edge Functions

### 7.1 تثبيت Supabase CLI

```bash
# Windows (PowerShell)
scoop install supabase

# macOS
brew install supabase/tap/supabase

# Linux
curl -sL https://github.com/supabase/cli/releases/latest/download/supabase_linux_amd64.tar.gz | tar xz
```

### 7.2 تسجيل الدخول

```bash
supabase login
```

سيفتح المتصفح - سجل الدخول بحساب Supabase.

### 7.3 ربط المشروع

```bash
# احصل على project-ref من Supabase Dashboard (Settings > General)
supabase link --project-ref xxxxx
```

### 7.4 نشر الدوال

```bash
# نشر دالة التأكيد
supabase functions deploy send-whatsapp-confirmation

# نشر دالة التذكيرات
supabase functions deploy send-booking-reminders
```

### 7.5 تعيين متغيرات البيئة

```bash
supabase secrets set WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
supabase secrets set WHATSAPP_ACCESS_TOKEN=your_permanent_access_token
```

---

## 🔗 الخطوة 8: إعداد Database Webhook

### 8.1 إنشاء Webhook

1. في Supabase Dashboard: **Database** > **Webhooks**
2. اضغط **"Create a new hook"**
3. املأ:
   - **Name**: `booking-approved`
   - **Table**: `bookings`
   - **Events**: ✅ UPDATE
   - **Type**: HTTP Request
   - **Method**: POST
   - **URL**: `https://xxxxx.supabase.co/functions/v1/send-whatsapp-confirmation`
     (استبدل xxxxx بـ project-ref)
   - **HTTP Headers**:
     ```json
     {
       "Authorization": "Bearer YOUR_ANON_KEY",
       "Content-Type": "application/json"
     }
     ```
4. اضغط **"Create webhook"**

---

## ⏰ الخطوة 9: إعداد CRON للتذكيرات

### 9.1 تفعيل pg_cron

1. **Database** > **Extensions**
2. ابحث عن `pg_cron`
3. فعّله

### 9.2 إنشاء CRON Job

1. **SQL Editor** > **New query**
2. الصق:

```sql
SELECT cron.schedule(
    'send-booking-reminders',
    '*/15 * * * *',
    $$
    SELECT net.http_post(
        url:='https://xxxxx.supabase.co/functions/v1/send-booking-reminders',
        headers:='{"Authorization": "Bearer YOUR_ANON_KEY", "Content-Type": "application/json"}'::jsonb
    );
    $$
);
```

3. استبدل `xxxxx` و `YOUR_ANON_KEY`
4. Run!

---

## ✅ الخطوة 10: الاختبار النهائي

### 10.1 اختبار كامل

1. أنشئ حجز جديد من الموقع
2. افتح لوحة التحكم
3. وافق على الحجز
4. **تحقق من واتساب** - يجب أن تصل رسالة تأكيد! 🎉

### 10.2 اختبار التذكير

1. أنشئ حجز بعد ساعتين من الآن
2. وافق عليه
3. انتظر حتى يحين وقت التذكير
4. يجب أن تصل رسالة تذكير!

---

## 🌐 الخطوة 11: النشر (اختياري)

### خيار 1: GitHub Pages

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/safari-stadium.git
git push -u origin main
```

ثم في GitHub:
- **Settings** > **Pages**
- **Source**: main branch
- **Save**

### خيار 2: Netlify

1. اذهب إلى https://netlify.com
2. اسحب مجلد المشروع
3. انتظر النشر
4. احصل على رابط الموقع!

---

## 🎉 تهانينا!

لقد أكملت الإعداد بنجاح! 🚀

### الخطوات التالية:

- [ ] أضف مصادقة للوحة التحكم
- [ ] خصص التصميم حسب علامتك التجارية
- [ ] أضف المزيد من الملاعب
- [ ] راجع سياسات الأمان

---

## 🆘 المساعدة

واجهت مشكلة؟

1. راجع قسم **استكشاف الأخطاء** في README.md
2. تحقق من Console في المتصفح
3. راجع Logs في Supabase Dashboard
4. افتح Issue في GitHub

---

**صُنع بـ ❤️ - حظاً موفقاً!**
