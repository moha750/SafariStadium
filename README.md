# ⚽ نظام حجز ملاعب سفاري

نظام حجز إلكتروني احترافي لملاعب كرة القدم مع لوحة تحكم إدارية.

## 📋 المحتويات

- [المميزات](#-المميزات)
- [التقنيات المستخدمة](#-التقنيات-المستخدمة)
- [هيكل المشروع](#-هيكل-المشروع)
- [الإعداد والتثبيت](#-الإعداد-والتثبيت)
- [إعداد Supabase](#-إعداد-supabase)
- [الاستخدام](#-الاستخدام)
- [النشر](#-النشر)

---

## ✨ المميزات

### الواجهة الرئيسية
- ✅ تصميم عصري وسريع الاستجابة
- ✅ عرض بطاقات الملاعب (سفاري 1 و سفاري 2)
- ✅ نموذج حجز تفاعلي
- ✅ التحقق من التوفر قبل الحجز
- ✅ دعم كامل للغة العربية (RTL)

### لوحة التحكم الإدارية
- ✅ عرض جميع الحجوزات
- ✅ إحصائيات فورية
- ✅ تصفية حسب الحالة والملعب
- ✅ الموافقة/الرفض بنقرة واحدة
- ✅ تحديث تلقائي

### قاعدة البيانات
- ✅ Supabase PostgreSQL
- ✅ Row Level Security (RLS)
- ✅ فهارس محسّنة للأداء
- ✅ Triggers تلقائية

---

## 🛠 التقنيات المستخدمة

### Frontend
- **HTML5** - هيكل الصفحات
- **CSS3** - تصميم عصري مع متغيرات CSS
- **JavaScript (ES6+)** - منطق التطبيق
- **Vanilla JS** - بدون أي إطار عمل

### Backend
- **Supabase** - قاعدة البيانات والمصادقة
- **PostgreSQL** - قاعدة بيانات علائقية

---

## 📁 هيكل المشروع

```
SafariStadium/
├── index.html                          # الصفحة الرئيسية
├── admin.html                          # لوحة التحكم
├── css/
│   ├── style.css                       # الأنماط الرئيسية
│   └── admin.css                       # أنماط لوحة التحكم
├── js/
│   ├── config.js                       # إعدادات Supabase
│   ├── supabase-client.js              # عميل قاعدة البيانات
│   ├── app.js                          # منطق الصفحة الرئيسية
│   ├── admin.js                        # منطق لوحة التحكم
│   └── utils.js                        # دوال مساعدة
├── supabase/
│   ├── migrations/
│   │   └── 001_create_bookings_table.sql  # إنشاء الجداول
│   └── config.toml                     # تكوين Supabase
├── .env.example                        # مثال متغيرات البيئة
├── .gitignore                          # ملفات Git المستبعدة
└── README.md                           # هذا الملف
```

---

## 🚀 الإعداد والتثبيت

### 1. استنساخ المشروع

```bash
git clone https://github.com/yourusername/safari-stadium.git
cd safari-stadium
```

### 2. إعداد ملف البيئة

```bash
# انسخ ملف المثال
cp .env.example .env

# افتح .env وأضف بياناتك
```

### 3. تحديث ملف التكوين

افتح `js/config.js` وأضف بيانات Supabase:

```javascript
const SUPABASE_CONFIG = {
    url: 'https://your-project.supabase.co',
    anonKey: 'your-anon-key'
};
```

---

## 🗄 إعداد Supabase

### 1. إنشاء مشروع جديد

1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. أنشئ مشروع جديد
3. انتظر حتى يكتمل الإعداد

### 2. تشغيل Migration

في Supabase Dashboard:

1. اذهب إلى **SQL Editor**
2. انسخ محتوى `supabase/migrations/001_create_bookings_table.sql`
3. الصق الكود ونفذه
4. تحقق من إنشاء الجدول في **Table Editor**

### 3. الحصول على API Keys

1. اذهب إلى **Project Settings** > **API**
2. انسخ:
   - `Project URL` → `SUPABASE_URL`
   - `anon public` → `SUPABASE_ANON_KEY`

---

## 💻 الاستخدام

### تشغيل المشروع محلياً

يمكنك استخدام أي خادم ويب محلي:

#### باستخدام Python:
```bash
python -m http.server 8000
```

#### باستخدام Node.js:
```bash
npx http-server -p 8000
```

#### باستخدام PHP:
```bash
php -S localhost:8000
```

ثم افتح المتصفح على: `http://localhost:8000`

### استخدام النظام

#### للعملاء:
1. افتح الصفحة الرئيسية
2. اختر الملعب
3. املأ نموذج الحجز
4. انتظر التأكيد من الإدارة

#### للإدارة:
1. افتح `admin.html`
2. راجع الحجوزات الجديدة
3. اضغط "موافقة" أو "رفض"

---

## 🌐 النشر

### النشر على GitHub Pages

1. ارفع المشروع إلى GitHub
2. اذهب إلى **Settings** > **Pages**
3. اختر branch: `main`
4. احفظ

**ملاحظة**: لا تنسَ إضافة `.env` و `js/config.js` إلى `.gitignore`

### النشر على Netlify

```bash
# تثبيت Netlify CLI
npm install -g netlify-cli

# نشر
netlify deploy --prod
```

### النشر على Vercel

```bash
# تثبيت Vercel CLI
npm install -g vercel

# نشر
vercel --prod
```

---

## 🔒 الأمان

### نصائح مهمة:

1. ✅ **لا تشارك** مفاتيح API في GitHub
2. ✅ استخدم **Row Level Security** في Supabase
3. ✅ قيّد الوصول إلى لوحة التحكم (أضف مصادقة)
4. ✅ استخدم HTTPS في الإنتاج
5. ✅ راجع سياسات CORS

### إضافة مصادقة للوحة التحكم (موصى به):

```javascript
// في admin.js
const ADMIN_PASSWORD = 'your-secure-password';

function checkAuth() {
    const password = prompt('أدخل كلمة المرور:');
    if (password !== ADMIN_PASSWORD) {
        alert('كلمة مرور خاطئة');
        window.location.href = 'index.html';
    }
}

checkAuth();
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا تظهر الحجوزات

**الحل**:
1. تحقق من إعدادات Supabase في `config.js`
2. افتح Console في المتصفح وابحث عن أخطاء
3. تحقق من RLS policies في Supabase

### المشكلة: خطأ CORS

**الحل**:
1. تأكد من تفعيل CORS في Supabase
2. أضف domain موقعك في Supabase Settings

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام التجاري والشخصي.

---

## 👨‍💻 المطور

تم تطوير هذا النظام كحل تجاري احترافي لإدارة حجوزات الملاعب الرياضية.

---

## 🤝 المساهمة

المساهمات مرحب بها! يرجى:

1. Fork المشروع
2. إنشاء branch جديد
3. Commit التغييرات
4. Push إلى Branch
5. فتح Pull Request

---

## 📞 الدعم

للمساعدة أو الاستفسارات:
- افتح Issue في GitHub
- راجع التوثيق الرسمي لـ [Supabase](https://supabase.com/docs)

---

**صُنع بـ ❤️ للمجتمع العربي**
